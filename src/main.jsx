import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App.jsx";
import "./index.css"; // Import global styles and fonts

ReactDOM.createRoot(document.getElementById("root")).render(
    <App />
);
